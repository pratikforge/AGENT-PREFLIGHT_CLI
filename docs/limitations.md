# Limitations

This is structural static analysis, not runtime verification. A verified result means a supported direct source pattern was found; it does not prove runtime behavior or operational readiness.

Aliases, dynamic values, malformed source, and unsupported frameworks can produce uncertainty or unsupported outcomes. The tool never executes scanned code, imports target modules, edits user source, auto-approves a rule, or generates an automatic patch.
